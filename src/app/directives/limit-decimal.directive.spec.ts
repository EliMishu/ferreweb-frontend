import { LimitDecimalDirective } from './limit-decimal.directive';

describe('LimitDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
